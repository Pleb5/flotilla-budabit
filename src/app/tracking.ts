export function setupTracking(): void {
  // No-op tracking initializer. Hook your analytics/tracking here if needed.
  // This stub exists to satisfy optional tracking imports in +layout.
  try {
    // Example: console.log("Tracking initialized")
  } catch {}
}
